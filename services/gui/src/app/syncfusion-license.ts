// Syncfusion license registration
// El spreadsheet se renderiza en un iframe aislado (backend HTML via CDN)
// donde la licencia se registra directamente con ej.base.registerLicense().
// Este archivo se mantiene como no-op para no romper app.config.ts.

export function registerSyncfusionLicense(): void {
    // No-op: sin dependencias Syncfusion en el frontend Angular
}
